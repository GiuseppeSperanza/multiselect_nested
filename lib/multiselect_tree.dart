library multiselect_tree;

import 'package:flutter/material.dart';

class MultiSelectTree extends StatelessWidget {
  const MultiSelectTree({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Ciao');
  }
}
